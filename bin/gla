#!/bin/sh
# : gla && git log --pretty=fuller --no-decorate --numstat --author=...  # --default=--color-moved
# : gla && git log --pretty=fuller --no-decorate --numstat --author=$(git config user.email)
g.py "$0" "$@"
