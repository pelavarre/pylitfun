#!/bin/sh

echo + : find . -type p >&2
echo "+ : find . -type d -empty -not -path '*/.git/*'" >&2
echo '+ : gsis && git status --ignored --short' "$@" >&2

: gsis && git status --ignored --short "$@"
