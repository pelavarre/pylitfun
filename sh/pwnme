#!/bin/bash

# usage: pwnme
# update this Git Clone

D=$(dirname "$0")/..
cd "$D"/ || exit 1

echo
echo cd "$(dirs -p |head -1)/"

echo
git log --oneline --no-decorate -3

echo
git fetch --prune --prune-tags --force
git rebase

echo
git log --oneline --no-decorate -3
