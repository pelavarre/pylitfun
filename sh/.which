#!/bin/bash

# sh/.which = Call Shell Which, but with -a and speaking ~/ to mean $HOME/

which -a "$@" | \
    awk -v HOME="$HOME" '(index($0, HOME) == 1){print "~" substr($0, 1 + length(HOME)); next} 1'
